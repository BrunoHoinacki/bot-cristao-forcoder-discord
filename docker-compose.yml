services:
  bot:
    build: .
    container_name: bot-cristao-discord
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
